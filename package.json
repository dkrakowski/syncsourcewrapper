{
  "name": "syncsourcewrapper",
  "version": "2.0.0",
  "description": "Wrapper for mySQL sync connection with sync-mysql package ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "dkrakowski",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^10.0.0",
    "sync-mysql": "^3.0.1"
  }
}
